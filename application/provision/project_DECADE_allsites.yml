api_version: 3
name: decade___REPLACED_BY_CURRENT_VERSION_NUMBER_WHEN_BUILDING_STARTUP_KITS___allsites
description: >
  DECADE consortium — STAMP (computational pathology) all-sites swarm project.
  TUD server, one client per DECADE site. Build with the STAMP Dockerfile:
    ./scripts/build/buildDockerImageAndStartupKits.sh \
        -p application/provision/project_DECADE_allsites.yml \
        -d docker_config/Dockerfile_STAMP
  (STAMP-ness comes from Dockerfile_STAMP overriding master_template.yml inside
  the image; this YAML keeps template_file: master_template.yml like all others.)

participants:
  - name: dl3.tud.de
    type: server
    org: TUD
    fed_learn_port: 8002
    admin_port: 8003

  # ── DECADE client sites ───────────────────────────────────────────────────
  # TODO: replace the placeholders below with the confirmed DECADE site
  # identifiers (name = NVFlare site name; must match each site's data folder
  # <SITE_NAME>/features + <SITE_NAME>/clini_table.csv). Add/remove entries to
  # match the real consortium roster.
  - name: SITE_A_1
    type: client
    org: SITE_A
  - name: SITE_B_1
    type: client
    org: SITE_B
  - name: SITE_C_1
    type: client
    org: SITE_C

  - name: jiefu.zhu@tu-dresden.de
    type: admin
    org: TUD
    role: project_admin

builders:
  - path: nvflare.lighter.impl.workspace.WorkspaceBuilder
    args:
      template_file: master_template.yml
  - path: nvflare.lighter.impl.template.TemplateBuilder
  - path: nvflare.lighter.impl.static_file.StaticFileBuilder
    args:
      config_folder: config
      scheme: http
      # docker.sh is generated on server/client/admin from this image name.
      docker_image: jefftud/odelia:__REPLACED_BY_CURRENT_VERSION_NUMBER_WHEN_BUILDING_STARTUP_KITS__
      overseer_agent:
        path: nvflare.ha.dummy_overseer_agent.DummyOverseerAgent
        overseer_exists: false
        args:
          # SERVER_NAME:FL_PORT:ADMIN_PORT — must match the server above.
          sp_end_point: dl3.tud.de:8002:8003
  - path: nvflare.lighter.impl.cert.CertBuilder
  - path: nvflare.lighter.impl.signature.SignatureBuilder
